INIT Init
NEXT Next

CONSTANTS
    Origin       = {"o1"}
    Rest         = {"a", "b"}
    Address      = {"addr1"}
    Pid          = {"pid1"}
    Value        = {"val"}
    Rid          = {"q1", "q2"}
    Implemented  = {"PromiseGet", "PromiseCreate", "PromiseSettle", "PromiseRegisterCallback",
                    "PromiseRegisterListener", "TaskGet", "TaskCreate", "TaskAcquire",
                    "TaskFence", "TaskHeartbeat", "TaskSuspend", "TaskFulfill",
                    "TaskRelease", "TaskHalt", "TaskContinue", "Timeout",
                    "ListenerDrain", "CallbackDrain"}
    Ttl          = {1}
    NoPid        = "nopid"
    NoAddr       = "noaddr"
    NoValue      = "noval"

    RetryTimeout = 2
    MaxTime      = 1
    MaxVersion   = 1
    MaxBatch     = 1

\* Not listed because they FAIL, and the failures are the result.
\* Reproduce one with:  ./check.sh Abstract WheelComplete
\*     WheelSound
\*     WheelComplete
\*     T_preserved_settled_promise_record
\*     T_consistent_promise_settlement_stamp

INVARIANT
    Agree
    AdvanceAgree
