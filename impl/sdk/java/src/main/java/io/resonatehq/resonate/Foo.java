package io.resonatehq.resonate;

/** Placeholder type to verify the build scaffold. Replace as the SDK port begins. */
public final class Foo {
    private Foo() {}

    public static String bar() {
        return "bar";
    }

    public static String baz(String input) {
        return input + "-baz";
    }
}
